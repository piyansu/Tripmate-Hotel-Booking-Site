<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>404 - Page Not Found</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #0f172a;
      color: #f1f5f9;
      overflow-x: hidden;
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      position: relative;
    }

    .bg-shapes {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      z-index: -1;
      overflow: hidden;
    }

    .shape {
      position: absolute;
      opacity: 0.05;
      filter: blur(4px);
    }

    .shape-1 {
      width: 300px;
      height: 300px;
      border-radius: 50%;
      background: linear-gradient(45deg, #3b82f6, #60a5fa);
      top: -100px;
      right: -50px;
    }

    .shape-2 {
      width: 200px;
      height: 200px;
      background: linear-gradient(135deg, #f97316, #fb923c);
      border-radius: 30% 70% 70% 30% / 30% 30% 70% 70%;
      bottom: -50px;
      left: -50px;
      animation: morph 15s linear infinite alternate;
    }

    .shape-3 {
      width: 150px;
      height: 150px;
      background: linear-gradient(135deg, #8b5cf6, #a78bfa);
      border-radius: 60% 40% 30% 70% / 60% 30% 70% 40%;
      top: 60%;
      right: 10%;
      animation: morph 12s linear infinite alternate;
    }

    .shape-4 {
      width: 80px;
      height: 80px;
      background: linear-gradient(135deg, #10b981, #34d399);
      border-radius: 30% 70% 50% 50% / 50% 50% 70% 30%;
      top: 20%;
      left: 10%;
      animation: morph 10s linear infinite alternate;
    }

    @keyframes morph {
      0% { border-radius: 30% 70% 70% 30% / 30% 30% 70% 70%; }
      25% { border-radius: 40% 60% 50% 50% / 50% 40% 60% 50%; }
      50% { border-radius: 60% 40% 30% 70% / 60% 30% 70% 40%; }
      75% { border-radius: 70% 30% 40% 60% / 30% 50% 50% 70%; }
      100% { border-radius: 30% 70% 70% 30% / 30% 30% 70% 70%; }
    }

    .grid-pattern {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-image: 
        linear-gradient(rgba(255, 255, 255, 0.02) 1px, transparent 1px),
        linear-gradient(90deg, rgba(255, 255, 255, 0.02) 1px, transparent 1px);
      background-size: 35px 35px;
      z-index: -1;
    }

    .error-container {
      max-width: 800px;
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 2rem;
      position: relative;
      z-index: 1;
    }

    .error-number {
      font-size: 180px;
      font-weight: 900;
      line-height: 1;
      color: #1e293b;
      letter-spacing: -10px;
      margin-bottom: 0.5rem;
      text-shadow: 1px 1px 0 #0f172a, 2px 2px 0 #1e293b, 3px 3px 0 #334155;
      display: flex;
      overflow: hidden;
    }

    .digit {
      animation: floatDigit 4s ease-in-out infinite;
    }

    .digit:nth-child(1) { animation-delay: 0s; }
    .digit:nth-child(2) { animation-delay: 0.5s; }
    .digit:nth-child(3) { animation-delay: 1s; }

    @keyframes floatDigit {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-10px); }
    }

    .error-number::before {
      content: "";
      position: absolute;
      width: 110%;
      height: 40%;
      background: linear-gradient(90deg, rgba(99, 102, 241, 0.2), rgba(236, 72, 153, 0.2));
      top: 30%;
      left: -5%;
      filter: blur(20px);
      border-radius: 50%;
      z-index: -1;
    }

    .lost-icon {
      font-size: 6rem;
      color: #64748b;
      margin: 1rem 0;
      animation: pulse 3s ease-in-out infinite;
    }

    @keyframes pulse {
      0%, 100% { transform: scale(1); opacity: 1; }
      50% { transform: scale(1.1); opacity: 0.8; }
    }

    .error-title {
      font-size: 2.5rem;
      font-weight: 700;
      margin-bottom: 1rem;
      text-align: center;
      color: #f8fafc;
      background: linear-gradient(135deg, #f8fafc 0%, #cbd5e1 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    .error-message {
      font-size: 1.2rem;
      line-height: 1.6;
      color: #94a3b8;
      max-width: 500px;
      text-align: center;
      margin-bottom: 2rem;
    }

    .back-home {
      display: inline-block;
      padding: 0.8rem 2rem;
      background: linear-gradient(135deg, #6366f1 0%, #4f46e5 100%);
      color: white;
      text-decoration: none;
      border-radius: 30px;
      font-weight: 600;
      font-size: 1rem;
      transition: all 0.3s ease;
      position: relative;
      overflow: hidden;
      box-shadow: 0 4px 15px rgba(99, 102, 241, 0.4);
    }

    .back-home:hover {
      transform: translateY(-3px);
      box-shadow: 0 8px 20px rgba(99, 102, 241, 0.5);
    }

    .back-home:active {
      transform: translateY(-1px);
    }

    .back-home i {
      margin-right: 0.5rem;
    }

    .compass {
      position: absolute;
      font-size: 15rem;
      color: rgba(203, 213, 225, 0.05);
      z-index: -1;
      animation: rotate 20s linear infinite;
    }

    @keyframes rotate {
      0% { transform: rotate(0deg); }
      100% { transform: rotate(360deg); }
    }

    .dots {
      position: absolute;
      width: 100%;
      height: 100%;
      z-index: -1;
    }

    .dot {
      position: absolute;
      width: 6px;
      height: 6px;
      border-radius: 50%;
      background-color: rgba(255, 255, 255, 0.05);
    }

    .dot:nth-child(1) { top: 10%; left: 10%; animation: float 3s ease-in-out infinite; }
    .dot:nth-child(2) { top: 20%; left: 80%; animation: float 4s ease-in-out infinite; }
    .dot:nth-child(3) { top: 80%; left: 15%; animation: float 5s ease-in-out infinite; }
    .dot:nth-child(4) { top: 70%; left: 80%; animation: float 3.5s ease-in-out infinite; }
    .dot:nth-child(5) { top: 30%; left: 60%; animation: float 4.5s ease-in-out infinite; }
    .dot:nth-child(6) { top: 50%; left: 30%; animation: float 5.5s ease-in-out infinite; }

    @keyframes float {
      0%, 100% { transform: translateY(0) scale(1); }
      50% { transform: translateY(-10px) scale(1.3); }
    }

    @media (max-width: 768px) {
      .error-number { font-size: 120px; }
      .error-title { font-size: 2rem; }
      .compass { font-size: 10rem; }
    }

    @media (max-width: 480px) {
      .error-number { font-size: 100px; }
      .error-title { font-size: 1.5rem; }
      .error-message { font-size: 1rem; }
      .compass { font-size: 8rem; }
    }
  </style>
</head>
<body>
  <div class="bg-shapes">
    <div class="shape shape-1"></div>
    <div class="shape shape-2"></div>
    <div class="shape shape-3"></div>
    <div class="shape shape-4"></div>
  </div>

  <div class="grid-pattern"></div>
  <div class="dots">
    <div class="dot"></div><div class="dot"></div><div class="dot"></div>
    <div class="dot"></div><div class="dot"></div><div class="dot"></div>
  </div>

  <i class="fa-solid fa-compass compass"></i>

  <div class="error-container">
    <div class="error-number">
      <div class="digit">4</div>
      <div class="digit">0</div>
      <div class="digit">4</div>
    </div>
    <i class="fa-solid fa-map-pin lost-icon"></i>
    <h1 class="error-title">Page Not Found</h1>
    <p class="error-message">
      Oops! It seems like you've ventured into uncharted territory.<br>
      The page you're looking for might have never existed.
    </p>
    <a href="/listings" class="back-home"><i class="fa-solid fa-house"></i> Back to Home</a>
  </div>
</body>
</html>
