<div class="auth-container">
  <div class="main">
    <input type="checkbox" id="chk" aria-hidden="true">
    
    <!-- Signup Form -->
    <div class="signup">
      <form class="auth-form" id="signup-form" action="/signup" method="post" onsubmit="return validatePassword()">
        <label for="chk" aria-hidden="true">Sign up</label>
        <input type="email" id="signup-email" name="email" placeholder="Email" required>
        <input type="text" id="signup-name" name="username" placeholder="Username" required>
        <input type="password" id="signup-password" name="password" placeholder="Password" required>
        <input type="password" id="signup-confirm" placeholder="Confirm Password" required>
        <span id="password-error" class="error-message"></span>
        <div class="terms-container">
          <input type="checkbox" id="terms" required>
          <label for="terms" class="terms-label">I agree to the <a href="#">Terms & Conditions</a></label>
        </div>
        <button type="submit">Create Account</button>
      </form>
    </div>

    <!-- Login Form -->
    <div class="login">
      <form class="auth-form" id="login-form" action="/login" method="post">
        <label for="chk" aria-hidden="true">Login</label>
        <input type="text" id="login-email" name="username" placeholder="Username" value="<%= previousUsername %>" required>
        <input type="password" id="login-password" name="password" placeholder="Password" required>
        <div class="form-actions">
          <div class="remember-me">
            <input type="checkbox" id="remember">
            <label for="remember" class="remember-label">Remember me</label>
          </div>
          <a href="#" class="forgot-password">Forgot password?</a>
        </div>
        <button type="submit">Sign In</button>
      </form>
      <div class="social-login">
        <p>Or sign in with</p>
        <div class="social-buttons">
          <button class="social-btn google">Google</button>
          <button class="social-btn facebook">Facebook</button>
        </div>
      </div>
    </div>
  </div>
</div>



.auth-container {
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 100vh;
  width: 100%;
  padding: 2rem; /* Add this to include padding in width/height calculation */
}

.auth-box {
  background-color: rgba(255, 255, 255, 0.95);
  border-radius: 10px;
  box-shadow: 0 15px 30px rgba(0, 0, 0, 0.2);
  width: 100%;
  max-width: 500px;
  overflow: hidden;
  padding: 2rem;
}

/* Tabs */
.tabs {
  display: flex;
  margin-bottom: 1.5rem;
  border-bottom: 1px solid #eee;
}

.tab-btn {
  background: none;
  border: none;
  cursor: pointer;
  font-size: 1.1rem;
  font-weight: 600;
  padding: 0.8rem 1rem;
  flex: 1;
  transition: all 0.3s ease;
  color: #555;
}

.tab-btn.active {
  color: #0066cc;
  border-bottom: 2px solid #0066cc;
}

.tab-content {
  display: none;
}

.tab-content.active {
  display: block;
  animation: fadeIn 0.5s ease;
}

@keyframes fadeIn {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}

/* Form Styles */
h2 {
  color: #333;
  margin-bottom: 0.5rem;
  text-align: center;
}

.subtitle {
  color: #666;
  text-align: center;
  margin-bottom: 1.5rem;
  font-size: 0.9rem;
}

.auth-form {
  margin-bottom: 1.5rem;
}

.form-group {
  margin-bottom: 1.2rem;
}

label {
  display: block;
  margin-bottom: 0.5rem;
  color: #555;
  font-weight: 500;
}

input[type="text"],
input[type="email"],
input[type="password"] {
  width: 100%;
  padding: 0.8rem;
  border: 1px solid #ddd;
  border-radius: 4px;
  font-size: 1rem;
  transition: border-color 0.3s;
}

input[type="text"]:focus,
input[type="email"]:focus,
input[type="password"]:focus {
  border-color: #0066cc;
  outline: none;
  box-shadow: 0 0 0 2px rgba(0, 102, 204, 0.2);
}

.form-actions {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 1.2rem;
  font-size: 0.9rem;
}

.remember-me {
  display: flex;
  align-items: center;
}

.remember-me input {
  margin-right: 0.5rem;
}

.forgot-password {
  color: #0066cc;
  text-decoration: none;
}

.forgot-password:hover {
  text-decoration: underline;
}

.btn-submit {
  width: 100%;
  padding: 0.8rem;
  background-color: #0066cc;
  color: white;
  border: none;
  border-radius: 4px;
  font-size: 1rem;
  font-weight: 600;
  cursor: pointer;
  transition: background-color 0.3s;
}

.btn-submit:hover {
  background-color: #0055aa;
}

/* Error message styling */
.error-message {
  color: #e74c3c;
  font-size: 0.85rem;
  margin-top: 0.3rem;
  display: block;
}

/* Social Login */
.social-login {
  text-align: center;
  margin-top: 1.5rem;
  padding-top: 1.5rem;
  border-top: 1px solid #eee;
}

.social-login p {
  color: #666;
  margin-bottom: 1rem;
  font-size: 0.9rem;
}

.social-buttons {
  display: flex;
  gap: 1rem;
}

.social-btn {
  flex: 1;
  padding: 0.7rem;
  border: 1px solid #ddd;
  border-radius: 4px;
  background-color: white;
  cursor: pointer;
  font-weight: 600;
  transition: background-color 0.3s;
}

.social-btn.google {
  color: #db4437;
}

.social-btn.facebook {
  color: #4267b2;
}

.social-btn:hover {
  background-color: #f5f5f5;
}

/* Terms and conditions */
.terms-container {
  display: flex;
  align-items: flex-start;
  margin-bottom: 1.2rem;
  font-size: 0.9rem;
}

.terms-container input {
  margin-right: 0.6rem;
  margin-top: 0.2rem;
}

.terms-container a {
  color: #0066cc;
  text-decoration: none;
}

.terms-container a:hover {
  text-decoration: underline;
}

/* Responsive Styles */
/* Tablet Media Queries (768px - 1024px) */
@media screen and (max-width: 1024px) {
  .auth-container {
    min-width: unset; /* Remove fixed min-width */
    padding: 1.5rem;
    zoom: 1; /* Reset zoom for better control */
  }

  .auth-box {
    max-width: 450px;
  }

  .tab-btn {
    font-size: 1rem;
    padding: 0.7rem 0.8rem;
  }
}

/* Phone Media Queries (up to 767px) */
@media screen and (max-width: 767px) {
  .auth-container {
    padding: 1rem;
    background-attachment: scroll; /* Better performance on mobile */
  }

  .auth-box {
    padding: 1.2rem;
    max-width: 400px;
    margin: 1rem auto;
  }

  h2 {
    font-size: 1.5rem;
  }

  .subtitle {
    font-size: 0.85rem;
    margin-bottom: 1.2rem;
  }

  .form-group {
    margin-bottom: 1rem;
  }

  input[type="text"],
  input[type="email"],
  input[type="password"] {
    padding: 0.7rem;
    font-size: 0.95rem;
  }

  .form-actions {
    flex-direction: column;
    align-items: flex-start;
    gap: 0.5rem;
  }

  .forgot-password {
    align-self: flex-end;
  }

  .terms-container {
    font-size: 0.8rem;
  }
}

/* Small Phone Media Queries (up to 480px) */
@media screen and (max-width: 480px) {
  .auth-container {
    padding: 0.5rem;
    min-height: calc(100vh - 1rem);
  }

  .auth-box {
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.15);
    padding: 1rem;
    border-radius: 8px;
  }

  .tabs {
    margin-bottom: 1.2rem;
  }

  .tab-btn {
    font-size: 0.9rem;
    padding: 0.6rem 0.5rem;
  }

  h2 {
    font-size: 1.3rem;
  }

  .subtitle {
    font-size: 0.8rem;
    margin-bottom: 1rem;
  }

  label {
    font-size: 0.9rem;
    margin-bottom: 0.3rem;
  }

  input[type="text"],
  input[type="email"],
  input[type="password"] {
    padding: 0.6rem;
    font-size: 0.9rem;
  }

  .btn-submit {
    padding: 0.7rem;
    font-size: 0.9rem;
  }

  .social-login p {
    font-size: 0.8rem;
  }

  .social-buttons {
    flex-direction: row; /* Keep buttons side by side */
  }

  .social-btn {
    padding: 0.6rem 0.4rem;
    font-size: 0.8rem;
  }
}

/* Very Small Phones (iPhone SE, etc.) */
@media screen and (max-width: 375px) {
  .auth-box {
    padding: 0.8rem;
  }

  .tab-btn {
    font-size: 0.85rem;
    padding: 0.5rem 0.3rem;
  }

  h2 {
    font-size: 1.2rem;
  }

  .subtitle {
    font-size: 0.75rem;
  }

  .social-buttons {
    gap: 0.5rem;
  }

  .error-message {
    font-size: 0.75rem;
  }
}

/* Landscape Mode for Phones */
@media screen and (max-height: 480px) and (orientation: landscape) {
  .auth-container {
    padding: 0.5rem;
    min-height: 100vh;
  }

  .auth-box {
    padding: 1rem;
    max-height: 85vh;
    overflow-y: auto;
  }

  .form-group {
    margin-bottom: 0.8rem;
  }

  h2 {
    font-size: 1.2rem;
    margin-bottom: 0.3rem;
  }

  .subtitle {
    margin-bottom: 0.8rem;
  }

  .btn-submit {
    padding: 0.6rem;
  }
}

/* Fix for minimal height issues */
@media screen and (max-height: 600px) {
  .auth-container {
    min-height: unset;
    padding-top: 4rem;
    padding-bottom: 4rem;
  }
}
